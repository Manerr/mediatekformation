{% extends "base.html.twig" %}

{% block title %}{% endblock %}
{% block stylesheets %}{% endblock %}
{% block top %}
<div class="container">
	<div class="text-left">
    <img alt="Bannière du site mediatekformation" src="{{ app.request.getBasePath()~'/banniere.jpg' }}" >
	</div>
	<nav class="navbar navbar-expand-lg navbar-light bg-light">
	 <div class="collapse navbar-collapse" id="navbarSupportedContent w-100">
	     <ul class="navbar-nav mr-auto w-100">
	         <li class="nav-item">
                 <a class="nav-link" href="{{ path('admin.accueil') }}">Accueil</a>
	         </li>
	         <li class="nav-item">
	             <a class="nav-link" href="{{ path('admin.formations') }}">Formations</a>
	         </li>
	         <li class="nav-item">
	             <a class="nav-link" href="{{ path('admin.playlists') }}">Playlists</a>
	         </li>   
	         <li class="nav-item">
	             <a class="nav-link" href="{{ path('admin.categories') }}">Categories</a>
	         </li>

          <li class="nav-item ms-auto">
              <a class="btn btn-primary" href="{{ path('accueil') }}">Front-office</a>
          </li>                  

           <li class="nav-item ms-2">

							<form action="{{ path('app_logout') }}" method="post">
					        <button type="submit" class="btn btn-warning">Déconnexion</button>
					    </form>             

          </li>                  
	     </ul>
	 </div>
	</nav>        
</div>
    <div class="container m-auto">
{% for label, flashes in app.flashes %}
    {% for flash in flashes %}
        {% if ( label == 'success' or label == 'sucess' ) %}
            <div class="alert alert-success alert-dismissible fade show">
                {{ flash }}
            </div>
        {% elseif ( label == 'error' or label == 'danger' ) %}
            <div class="alert alert-danger alert-dismissible fade show">
                {{ flash }}
            </div>
        {% endif %}
    {% endfor %}
{% endfor %}
    </div>

{% endblock %}

{% block footer %}
    <div class="container text-center">
        <footer>
            <hr>
            <p><small><i>
                Consultez nos <a class="link-secondary" href="{{ path('cgu') }}">Conditions Générales d'Utilisation</a>
            </i></small></p>
            
        </footer>
    </div>
{% endblock %}
{% block javascripts %}{% endblock %}